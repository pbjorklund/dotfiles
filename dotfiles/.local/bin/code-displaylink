#!/bin/bash
# VSCode wrapper for DisplayLink monitors
# Disables GPU acceleration to prevent crashes on USB-connected displays

exec /usr/bin/code \
    --disable-gpu \
    --disable-hardware-acceleration \
    --disable-gpu-sandbox \
    --no-sandbox \
    "$@"
