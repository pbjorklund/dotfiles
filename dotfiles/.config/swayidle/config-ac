# Swayidle configuration for AC power
# Lock screen and turn off display after 15 minutes, suspend after 1.5 hours

# Lock screen after 15 minutes of inactivity
timeout 900 'swaylock -f'

# Turn off display 30 seconds after lock, turn back on when active
timeout 930 'hyprctl dispatch dpms off' resume 'hyprctl dispatch dpms on'

# Suspend after 1.5 hours (5400 seconds) of inactivity
timeout 5400 'systemctl suspend'

# Lock before system sleep/suspend (manual or lid close)
before-sleep 'swaylock -f'

# Ensure display management is handled by scripts, not swayidle
after-resume 'sleep 1'
