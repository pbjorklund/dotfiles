[alias]
co = checkout
ci = commit
st = status
br = branch
hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
type = cat-file -t
dump = cat-file -p
[user]
name = Patrik Björklund
email = patrik@cognit.se
[color]
branch = auto
diff = auto
status = auto
[color "status"]
added = yellow
changed = green bold
untracked = blue bold
[credential "https://github.com"]
helper = libsecret
[credential "https://gist.github.com"]
helper = libsecret
[credential]
	helper = "!f() { op item get \"$1\" --fields credential --reveal 2>/dev/null || echo; }; f"
